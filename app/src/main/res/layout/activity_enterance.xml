<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_enterance"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/clouds"
    >

    <TextView
        android:text="@string/titleEnteranceTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/titleEnteranceTextView"
        tools:textColor="@android:color/black"
        android:layout_marginTop="133dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textSize="50sp" />

    <ImageView
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:id="@+id/enteranceImageView"
        android:layout_marginTop="131dp"
        android:src="@drawable/your_job_close_than_ever"
        android:layout_below="@+id/titleEnteranceTextView"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/enterance_pic" />

</RelativeLayout>
